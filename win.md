


## 双屏显示/扩展模式
在Windows系统中设置屏幕扩展（也叫“双屏显示”或“扩展模式”）非常简单。这里有两种最常用的方法，你可以根据自己的习惯选择。

### 🎯 方法一：使用快捷键（最快、最推荐）

这是最快、最直观的方法，适用于所有主流Windows版本（Windows 10/11等）。

1.  同时按下键盘上的 **`Win`键（Windows徽标键） + `P`键**。
2.  此时屏幕右侧会弹出一个名为“投影”的侧边栏，里面有四个选项 ：
    *   **仅电脑屏幕**：只使用当前的主显示器。
    *   **复制**：两个屏幕显示一模一样的内容，常用于演示或会议 。
    *   **扩展**：将两个屏幕拼接成一个大的桌面，你可以将窗口在两个屏幕之间来回拖动，这是最高效的多任务办公模式 。
    *   **仅第二屏幕**：关闭主显示器，只使用外接的屏幕。

    你需要做的，就是用鼠标点击选择 **“扩展”**，设置即时生效。

### ⚙️ 方法二：通过系统“显示设置”（功能更全）

如果你需要对屏幕进行更细致的调整，比如排列位置、设置主副屏、调整分辨率等，可以通过这个路径进入 。

1.  在桌面空白处点击**鼠标右键**，选择 **“显示设置”** 。
2.  在打开的设置窗口中，你会看到代表你两个显示器的图标（通常标记为“1”和“2”）。如果没看到，可以先点击右下方的 **“检测”** 按钮 。
3.  向下滑动找到 **“多显示器”** 一栏，点击下拉菜单，选择 **“扩展这些显示器”** 。
4.  最后点击 **“保留更改”** 或 **“应用”** 即可 。
5.  **（可选操作）调整屏幕位置**：在窗口顶部的布局图中，你可以用鼠标拖动两个显示器图标，让它们的位置与实际摆放位置（如左/右）一致，这样鼠标在屏幕间移动时才不会“卡住” 。

### 💡 如果遇到问题怎么办？

如果按照以上步骤操作后，外接屏幕没有反应，可以从以下几个方面排查：
*   **检查连接**：确保视频线（如HDMI、DP线）两端都已牢固插入，并且外接显示器电源已打开 。
*   **选择正确信号源**：按一下外接显示器上的物理按键，在菜单中确保选择了正确的输入源（例如，插了HDMI线，就要把信号源切换到“HDMI”）。
*   **更新显卡驱动**：过时或损坏的显卡驱动是导致无法识别第二屏幕的常见原因。你可以通过“设备管理器”更新驱动，或使用驱动管理软件进行检测 。

你更习惯用快捷键，还是想在设置界面里慢慢调整？告诉我你的偏好，如果遇到任何问题（比如屏幕没反应），我可以提供更具体的排查步骤。
